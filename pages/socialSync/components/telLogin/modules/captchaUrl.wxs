
module.exports = function (captchaPath, globalCaptchaPath) {
  captchaPath = captchaPath || ''
  globalCaptchaPath = globalCaptchaPath || ''

  return (captchaPath ? (globalCaptchaPath + captchaPath) : captchaPath)
}
